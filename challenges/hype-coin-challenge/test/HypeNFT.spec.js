const { expect } = require('chai');
const { ethers } = require('hardhat');

describe.skip('HypeNFT', () => {
  let nft;
  let deployer, user;

  beforeEach(async () => {
  });

  it('sets up storage vars', async () => {
  });

  it('mints 5 tokens for the caller, with tokenIds 1...5', async () => {
  });

  it('funds the contract owner\'s wallet with ETH on successful mints', async () => {
  });
});
